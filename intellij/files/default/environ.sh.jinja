########################################################################
# File managed by Salt at <{{ source }}>.
# Your changes may be overwritten.
########################################################################
{%- set path = '{}{}'.format(appname, edition) %}

export PATH=${PATH}:{{ path }}
{{ '' if not environ else environ|yaml(False) }}
